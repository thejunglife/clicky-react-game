import React from 'react'
// import GameContext from '../../utils/GameContext'

const Header = () => {
  
  return (
    <>
    <h1>Welcome to Clicky Game!</h1>
    <h2>Click an image to earn points, but don't click more than once!</h2>
    </>
  )
}

export default Header